Manual de uso.

* Ejecutar el archivo AngularElementsCreator.exe

* Si se realizan modificaciones en el codigo, para realizar el ejecutable se instala la siguiente aplicacion corriendo el siguiente comando:
- pip install auto-py-to-exe "Se debe tener instalado Python"
* Para ejecutar la aplicacion instalada, se ejecuta el siguiente comando: 
- auto-py-to-exe